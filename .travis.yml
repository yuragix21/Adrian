addons:
  sonarcloud:
    organization: "yuragix21"
    token:
      secure: "travis encrypt --pro 66375e0b9e4959277423804c0dacdda32d66950f" # encrypted value of your token

script:
  # the following command line builds the project, runs the tests with coverage and then execute the SonarCloud analysis
  - sonar-scanner